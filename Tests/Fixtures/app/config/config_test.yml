imports:
    - { resource: config.yml }
    - { resource: parameters.yml }

symfony_rollbar:
  enable: true
  rollbar:
    access_token: '%env(ROLLBAR_ACCESS_TOKEN)%'
    environment: '%kernel.environment%'
